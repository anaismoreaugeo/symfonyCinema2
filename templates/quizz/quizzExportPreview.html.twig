{% extends 'base.html.twig' %}

{% block title %}Hello CsvExportController!{% endblock %}

{% block body %}

    <div class="example-wrapper">
        <h1>Encoded</h1>
        <div>
            After :
            {{ dump(json_content) }}
        </div>
        <a href="{{ path('app_quizz_encode') }}" download="{{ listOfQuiz }}">Download Link</a>


    </div>
{% endblock %}